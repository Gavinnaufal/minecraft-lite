/** Ukuran chunk dalam blok (sumbu X) */
export const CHUNK_SIZE_X: number = 16;

/** Ukuran chunk dalam blok (sumbu Z) */
export const CHUNK_SIZE_Z: number = 16;

/** Tinggi maksimum dunia dalam blok */
export const CHUNK_HEIGHT: number = 128;

/** Default render distance (jumlah chunk) */
export const DEFAULT_RENDER_DISTANCE: number = 6;

/** Gravitasi (3x gravitasi bumi, khas voxel game) */
export const GRAVITY: number = -9.8 * 3;

/** Level permukaan air dalam blok */
export const WATER_LEVEL: number = 40;

/** Kecepatan gerak player dalam blok/detik */
export const PLAYER_SPEED: number = 5.0;

/** Kecepatan lompat awal player dalam blok/detik */
export const PLAYER_JUMP_FORCE: number = 8.0;
